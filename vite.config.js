import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // This configuration file makes the Vite setup more explicit,
  // ensuring a stable build process on platforms like Netlify.
  // Vite will automatically pick up your tailwind.config.js and postcss.config.js.
});
